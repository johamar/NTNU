package com.group7.krisefikser.enums;

/**
 * Enum representing different themes for general information.
 * This enum is used to categorize general information
 * into different themes
 * for better organization and retrieval.
 */
public enum Theme {
  BEFORE_CRISIS,
  DURING_CRISIS,
  AFTER_CRISIS,
}
